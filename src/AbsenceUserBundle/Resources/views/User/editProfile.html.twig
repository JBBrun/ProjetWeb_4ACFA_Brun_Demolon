{#{% extends "AbsenceBundle::main.html.twig" %}#}
{% extends "AbsenceBundle::template.html.twig" %}


{% block titre %}

    <spam  class="page-header">Modification de l'utilisateur</spam>

{% endblock %}


{% block body %}
    <div class="mid-block">
    <SCRIPT language=javascript>
        var chaine = "{{ dataUser.username }}";
        var chaine2 = "{{ dataUser.email }}";


        $( document ).ready(function() {
            document.getElementById('EditProfile_Type_username').value = chaine;
            document.getElementById('EditProfile_Type_email').value = chaine2;

        });

    </SCRIPT>

    {% for flashMessage in app.session.flashbag.get('error') %}

        {{ flashMessage }}

    {% endfor %}

    {% if form is defined %}
        <div class="centerForm">
     {{ form(form) }}
        </div>
    {% endif %}
    <div class="centerForm">
        <div class="cent">
            <a href="{{ path('absence_user_list') }}">
                <button type="button" class="btn btn-primary ">Retour liste</button>
            </a>
        </div>
    </div>
    </div>
{% endblock %}